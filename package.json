{
  "name": "chenda-functions",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "serve": "firebase emulators:start --only functions,hosting",
    "deploy": "firebase deploy --only functions,hosting",
    "lint": "echo 'No lint configured'"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^5.0.1",
    "node-fetch": "^3.3.2",
    "cors": "^2.8.5"
  }
}